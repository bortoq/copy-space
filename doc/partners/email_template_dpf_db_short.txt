file: doc/partners/email_template_dpf_db_short.txt
date: 2026-05-05
title: Email template (short) — DPF for DB/Analytics data movement

NOTE: partner-facing / non-technical template. For technical docs see `doc/README.md`.

Subject: Paid feasibility: deterministic data movement for compaction / reorder

Hi <Name>,

I’m Dmitri Bortoq. I’m working on Copy-Space / DPF: a deterministic data-movement model where the core operation is a scheduled copy (copy n bits from src to dst).

This may be relevant to DB/analytics workloads like:
- selection vector compaction (pack after filter)
- reorder / hash partition / materialization (permute)

Quick demo (outputs CSV):
  make bins
  scripts/demo_db.sh > /dev/null 2> tmp/demo.stderr
  cat tmp/demo.csv

Repo: https://github.com/bortoq/copy-space
One-pager: doc/partners/one_pager_dpf_db.txt

If you can share a rough workload shape (record width, selectivity, partition count, latency target), I can do a short paid feasibility sprint:
map → measure → estimate required bandwidth + main hardware risks (rough, explicit assumptions).

Would you be open to a short email exchange? What parameters should I use?

Best,
Dmitri Bortoq
bortoq@gmail.com
Telegram: @the_arctium
