file: doc/startup/email_template_dpf_db.txt
date: 2026-05-05
title: Email template — DPF (Deterministic Data Movement Fabric) for DB/Analytics

SUBJECT (option 1):
Paid feasibility: deterministic data-movement fabric for compaction / reorder

SUBJECT (option 2):
Question: compaction/partition data-movement — can I model your workload?

BODY (short, simple English):

Hi <Name>,

I’m Dmitri Bortoq. I’m working on Copy-Space / DPF: a deterministic data-movement fabric model where the core operation is a scheduled copy (copy n bits from src to dst).

This is aimed at data-movement bottlenecks that DB/analytics systems hit:
- compaction after filter (selection vectors)
- reorder / hash partition / materialization
- bulk buffer movement

Quick demo (outputs CSV):
  make bins
  scripts/demo_db.sh > /dev/null 2> tmp/demo.stderr
  cat tmp/demo.csv

One-pager:
  doc/startup/one_pager_dpf_db.txt

Repo:
  https://github.com/bortoq/parallel-computing

If your team has a representative workload shape (record width, selectivity, partition count, latency target), I can do a short paid feasibility sprint:
- map it to DPF primitives
- measure throughput/latency in the model
- estimate required bandwidth + main ASIC/FPGA risks (rough, with explicit assumptions)

Would you be open to a short email exchange? If yes, what workload parameters should I use?

Best,
Dmitri Bortoq
bortoq@gmail.com
Telegram: @the_arctium
