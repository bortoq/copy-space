Снэпшот сессии: рефакторинг проекта + разрезание mkimage legacy + TDD A-пункты (2026-05-04)
Контекст проекта
Проект Copy-Space / Forth0, std7_fixed baseline, добавлены 2a (ADD24/EQ24/LT24) и старт 2b (EQ24P + LITAP/LITBP/LITRP + VAR_AP/BP/RP + OFFTAB).
Работает scripts/test_all.sh (все тесты PASS).
Добавлен vmrep (bits-per-tick reporting) и бенчи bulk/pack/permute.
Введены TDD-тесты scripts/tdd/* для инженерных улучшений A:
A.1 диагностика VM_ERR (tick/slot/n/dst/src)
A.2 стандартизация test scratch + публикация через ART
A.3 удобство отладки: vmrep tools + fail bundles
scripts/tdd/run_all.sh теперь включает:
test_vmrep_tools.sh (PASS)
test_vmerr_diag.sh (PASS)
test_scratch_abi.sh (PASS после ловли stderr и печати TESTSCR)
test_scratch_artifacts.sh (PASS после ART/TESTG -> scratch)
test_fail_bundle.sh (PASS после fail bundles + FORCE_BAD_EXP)
Текущее состояние TDD A
A.1: в vm_tick() добавлена печать диагностической строки при bounds error:
VM_ERR: tick=... slot=... n=... dst=... src=... kind=... space_bits=...
vmrun возвращает nonzero на VM_ERR.
A.2:
mkimage_std7_fixed печатает в stderr:
TESTSCR_BASE(byte)=...
TESTSCR_SIZE(byte)=...
ART(byte)=...
Scratch вычисляется как TESTSCR_BASE = WORDS_BASE - TESTSCR_SIZE_BITS, TESTSCR_SIZE_BYTES=8192.
Overlap-check усилен: pool не должен заходить на scratch (pool_end + guard > TESTSCR_BASE).
В ART:
ART[43] (TESTG) == TESTSCR_BASE (зафиксировано решением пользователя)
добавлены ART[63] = TESTSCR_BASE, ART[64] = TESTSCR_END (END = base + size_bits)
A.3:
scripts/test_all.sh поддерживает:
fail bundle в tmp/fail/<name>_<timestamp>/
FORCE_BAD_EXP=1 для намеренного падения и проверки fail bundles
вывод vmrep блоков из логов (опционально по env)
Рефакторинг PASS1 (выполнен)
Цель: уменьшить гигантские файлы и снизить ошибки итераций.

Сделано:

vmrep вынесен из src/vm/space.c в отдельный модуль:
src/vm/diag/vmrep.c / src/vm/diag/vmrep.h
src/vm/space.c теперь включает diag/vmrep.h
Фиксы includes:
в space.c добавлен <inttypes.h> из-за PRIu64
в vmrep.c добавлен <stdio.h> из-за fprintf/stderr
mkimage_std7_fixed перестроен:
src/mkimage/mkimage_std7_fixed.c стал wrapper с main() → std7_fixed_legacy_main()
большой исходник перенесён в src/mkimage/std7_fixed/legacy.c и main() переименован в std7_fixed_legacy_main()
Обновлён Makefile (сгенерирован новый Makefile):
собирает vm tools, токен-тесты, mkimage
автоматически включает src/vm/diag/vmrep.c если существует
собирает mkimage как wrapper + src/mkimage/std7_fixed/*.c (после дальнейшего распила)
Проверка: make test PASS.

Рефакторинг PASS2.1 (выполнен)
Начали разрезать legacy.c.
Вынесен layout в:
src/mkimage/std7_fixed/layout.c / layout.h
legacy.c теперь вызывает std7_fixed_compute_layout(...) и получает:
WORDS_BASE
TESTSCR_BASE
TESTSCR_SIZE_BYTES и TESTSCR_SIZE_BITS (снова используется для ART[64])
pool_end и overlap-check через layout
После правок:

Было несколько warning про unused переменные/функции (решение: удалять или (void) глушить).
После добавления ART[63]/ART[64] переменная TESTSCR_SIZE_BITS снова понадобилась.
Текущая задача (следующий шаг)
Начать PASS2.2: вынести ART/ABI из legacy.c в отдельные файлы:

src/mkimage/std7_fixed/addrs.h — структура std7_addrs_t со всеми адресами, которые пишутся в ART
src/mkimage/std7_fixed/artifacts.h — enum индексов ART (включая:
ART_TESTG=43 указывает на TESTSCR_BASE
ART_TESTSCR_BASE=63
ART_TESTSCR_END=64 (END))
src/mkimage/std7_fixed/artifacts.c — функция std7_fixed_write_artifacts(vm, ART, &A), которая пишет ART по A
После этого legacy.c станет сильно меньше: длинный блок vm_write_uint(&vm, ART + ...) будет заменён на заполнение std7_addrs_t и вызов std7_fixed_write_artifacts().

Зафиксированные решения по ABI scratch
ART[43] (TESTG) == TESTSCR_BASE
ART[64] == TESTSCR_END (END, 1 past end)
Размер scratch: TESTSCR_SIZE_BYTES = 8192 (на текущем этапе)
Запрос пользователя
Пользователь готов прислать src/mkimage/std7_fixed/legacy.c целиком.
После получения целого файла ассистент должен:

сгенерировать addrs.h, artifacts.h, artifacts.c
вернуть обновлённый legacy.c (или точный unified diff), где ART запись вынесена в artifacts
обновить Makefile (если нужно) так, чтобы mkimage собирался из новых модулей
обеспечить PASS:
make test
scripts/tdd/run_all.sh
Доп. организационное правило (для будущих документов)
Пользователь попросил: в следующий раз в заголовке документов указывать имя файла (например doc/xx_name.md — ... — date).





