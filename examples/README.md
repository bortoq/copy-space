# Examples

## demands.csv
A tiny example demand list for a first run.

Run a pilot in one command:
  ./scripts/scheduler/pilot_run.sh --csv examples/demands.csv --bw 256 --outdir tmp/pilot_example

Or step-by-step (see doc/partners/quickstart_pilot.md).
