глоссарий жарго

T01 — коммит (атомарный набор изменений в репозитории, который должен собираться и проходить тесты)
T02 — todo B (раздел B в doc/20_todo.md: доведение 2b-указателей до пригодности)
T03 — спека/spec-документ (документ, который фиксирует контракт/формат/семантику так, чтобы потом не было разночтений)
T04 — патч/unified diff (текстовое представление изменений файлов, которое можно применить к репозиторию)
T05 — block pointer (указатель-адрес в space, выровненный на 32 бита: low5=0; используется как “база блока”)
T06 — raw bit pointer (указатель-адрес в space на произвольный бит, может быть невыровненным)
T07 — space (память VM, бит-адресуемый массив, где лежат код и данные)
T08 — bitaddr (битовый адрес в space, единица адресации = 1 бит)
T09 — слово/word (страница/микрокод, который исполняется в VM как “слово” Forth0)
T10 — токен/token (единица входного языка токен-компилятора: строковая команда, которая компилируется в вызов words)
T11 — token-test (тест, который генерит токены → компилирует → запускает → проверяет результат в TESTSCR/TESTG)
T12 — mkimage (утилита, генерирующая начальный образ space.bin для baseline std7_fixed)
T13 — ABI (контракт: какие адреса/структуры/артефакты где лежат и что означают)
T14 — ART (таблица артефактов: массив адресов/значений ABI, публикуемый mkimage)
low5 (младшие 5 бит адреса; T15)
32-bit aligned (адрес кратен 32 битам, т.е. addr % 32 == 0; T16)
вертикальные срезы” (небольшая функциональность + тест + PASS; T16),
“frontend” (внешний способ описания программ; T17),

PPA (Performance/Power/Area; оценка производительности/энергии/площади, T18)
ASIC (application‑specific integrated circuit; заказной чип, T19)
FPGA (field‑programmable gate array; программируемая логика, T20)
RTL (register‑transfer level; описание логики на уровне регистров, T23)
синтез (synthesis; преобразование RTL в логические элементы, T24)
interconnect/wiring (провода и топология, T26)
T22
