file: doc/startup/email_template_dpf_db_technical.txt
date: 2026-05-05
title: Email template (technical) — DPF for DB/Analytics data movement

Subject: Feasibility (paid): compaction / reorder data movement with deterministic copy fabric

Hi <Name>,

I’m Dmitri Bortoq. I’m working on Copy-Space / DPF: a deterministic data-movement fabric model where the core operation is a scheduled copy (copy n bits from src to dst), executed in ticks with explicit scheduling.

Quick demo (produces 3 CSV rows: pack/permute/bulkcopy):
  make bins
  scripts/demo_db.sh > /dev/null 2> tmp/demo.stderr
  cat tmp/demo.csv

Key metric in CSV:
  vmrep_avg_bits_uniq_dst_per_tick  = effective unique destination bits written per tick (i.e., useful write throughput)

Default demo shows contrast:
- pack/permute: ~32768 bits/tick = 4096 bytes/tick
- bulkcopy:     ~524288 bits/tick = 65536 bytes/tick (64KB/tick)

Repo:
  https://github.com/bortoq/parallel-computing
One-pager:
  doc/startup/one_pager_dpf_db.txt

If you share a representative workload shape (record width, selectivity, partition count, target latency/throughput), I can do a short paid feasibility sprint:
- map it to compaction/reorder primitives
- measure throughput/latency in the model
- estimate required memory bandwidth + main ASIC/FPGA risks (rough, with explicit assumptions)

Would you be open to a short email exchange? What workload parameters should I use?

Best,
Dmitri Bortoq
Email: bortoq@gmail.com
Telegram: @the_arctium
